version: "3.1"

intents:
  - pregunta_abierta
  - consulta_tecnica
  - ayuda

responses:
  utter_default:
  - text: "Procesando tu consulta..."

actions:
  - action_procesar_con_llm

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
